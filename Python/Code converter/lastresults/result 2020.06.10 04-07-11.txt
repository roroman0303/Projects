#Было убрано объявление библиотеки:
# #include <stdio.h>

#Было убрано объявление библиотеки:
# #include <conio.h>

#Строка была убрана:
# using namespace std;

#Было преобразовано объявление функции:
# int main()
#Начало главной исполняемой программы
def main():

#Было преобразовано объявление массива:
#     float x[10];
#В Python присутствует динамическая типизация
#В Python массив данных обычно представляется в виде списка
    x = [None] * 10

#Было преобразовано объявление переменных:
#     float max, min;
#В Python присутствует динамическая типизация
#Нет необходимости объявлять переменные заранее

#Был преобразован цикл:
#     for (int i=0; i < 10 ; i++)
#Цикл со счетчиком i от 0 до 10
    for i in range(0, 10):

#Был преобразован консольный вывод:
#         cout+ "x["+(i+1)+"]= ";
        print( "x["+(i+1)+"]= ")

#Был преобразован консольный ввод:
#         cin>>x[i];
        input(x[i])

#Была преобразована операция присвоения:
#     max=x[0];
#Объекту max было присвоено значение x[0]
    max=x[0]

#Была преобразована операция присвоения:
#     min=x[0];
#Объекту min было присвоено значение x[0]
    min=x[0]

#Был преобразован цикл:
#     for(int i=1; i < 10 ; i++)
#Цикл со счетчиком i от 1 до 10
    for i in range(1, 10):

#Была преобразована условная конструкция:
#         if (x[i]>max)
#Ветвление с условием x[i]>max
        if x[i]>max:

#Была преобразована операция присвоения:
#             max = x[i];
#Объекту max было присвоено значение x[i]
            max = x[i]

#Была преобразована условная конструкция:
#         if (x[i]<min)
#Ветвление с условием x[i]<min
        if x[i]<min:

#Была преобразована операция присвоения:
#             min=x[i];
#Объекту min было присвоено значение x[i]
            min=x[i]

#Была преобразована операция присвоения:
#     x[0] = max+min;
#Объекту x[0] было присвоено значение max+min
    x[0] = max+min

#Был преобразован консольный вывод:
#     cout+"max = "+max;
    print("max = "+max)

#Был преобразован консольный вывод:
#     cout+"min = "+min;
    print("min = "+min)

#Был преобразован цикл:
#     for(int i=0; i < 10 ; i++)
#Цикл со счетчиком i от 0 до 10
    for i in range(0, 10):

#Был преобразован консольный вывод:
#         cout+"x["+(i+1)+"] = "+x[i];
        print("x["+(i+1)+"] = "+x[i])

#Был преобразован оператор возврата:
#     return 0;
#Конец исполняемой программы
    return 0
main()

